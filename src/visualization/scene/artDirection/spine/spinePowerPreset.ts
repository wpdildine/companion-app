/**
 * Spine power preset: sleep/idle fade multipliers (optional).
 * Composed by spineArtDirection.ts; do not import from render/build.
 */

export const SPINE_POWER_PRESET = {
  // Placeholder for future sleep/idle fade multipliers if needed.
} as const;
