/**
 * Canonical app-authored strings for Scripted Response Seam v1 (A-scope answer slot only).
 */

/** Shown when the model path completes with empty settled output. */
export const SCRIPTED_EMPTY_OUTPUT_MESSAGE = 'No answer generated';

/**
 * Augmentation prefix for front-door clarify_entity presentation (additive framing over payload names).
 */
export const SCRIPTED_CLARIFY_ENTITY_PREFIX = 'Which did you mean?\n\n';
