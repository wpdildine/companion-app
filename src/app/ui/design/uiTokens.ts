/**
 * UI design tokens: spacing, radius, typography, motion.
 * Place for future tokens/recipes and skin maps.
 */

/** Base spacing unit (from DeconPanel and related). */
export const UNIT = 8;

/** Panel/section radius. */
export const RADIUS_PANEL = 10;
export const RADIUS_SM = 8;

/** Typography (sizes/line-heights from panel shell). */
export const H1 = 20;
export const H1_LINE_HEIGHT = 26;
export const H2 = 16;
export const H2_LINE_HEIGHT = 22;
export const META = 12;
export const META_LINE_HEIGHT = 16;

/** Motion (dismiss gesture). */
export const DISMISS_HOLD_MS = 190;
export const DISMISS_SWIPE_PX = 28;
