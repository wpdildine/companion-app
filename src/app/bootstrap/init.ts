/**
 * App bootstrap: one-off init (rehydrate, permissions, migrations).
 * Boot logic that does not belong in UI composition.
 */

// Placeholder: move getOnDeviceModelPaths, copyBundlePackToDocuments wiring, etc. here as needed.

export function runBootstrap(): void {
  // No-op for now; call from App or index when extracting boot from App.tsx.
}
