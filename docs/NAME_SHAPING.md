# Name Shaping (removed)

The paused **Name Shaping** prototype previously lived under `src/app/_experimental/nameShaping/`. That tree has been **removed** from the repository as part of dormant-surface normalization (visualization must not import app experimental code; see `docs/ARCHITECTURE.md`).

Spine touch semantics are documented in **docs/APP_ARCHITECTURE.md** and **docs/INTERACTION_CONTRACT.md** (`InteractionBand`, rules/neutral/cards zones, center hold). Prior design notes for sound-shape signatures and resolver ideas exist only in git history if needed for a future reboot.
